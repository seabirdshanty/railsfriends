<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">

    <%= link_to 'The World: Friends', friends_path, class:"navbar-brand" %>
    <% #link_to 'CC2', font_path, class:"navbar-brand" %>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'About Us', home_about_path, class:"nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to 'New Friend', new_friend_path, class:"nav-link" %>
        </li>
      </ul>

      <ul class="navbar-nav d-flex">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-fill"></i> Welcome, <%= current_user.first_name %>!
          </a>
          <ul class="dropdown-menu">
            <li> <%= link_to "Show Profile", current_user, class:"dropdown-item", data: { turbo: false } %></li>
            <li> <%= link_to 'Edit Profile', edit_user_registration_path, class:"dropdown-item", data: { turbo: false } %></li>
            <li><%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class:"dropdown-item" %></li>
          </ul>
        </li>
      </ul>
     
    </div>
  </div>
</nav>

<% #if user_signed_in? %>
<% #end %>